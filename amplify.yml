version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - cd client
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: client/dist
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
          - client/node_modules/**/*
